<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hf.mapper.CommentMapper">

<!-- 댓글입력 -->
    <insert id="commentCheck">
    <![CDATA[
        INSERT INTO Comments (commentid, userid, referenceid, commenttext, commentlikecnt, commenttype, commenttime)
        VALUES (comments_seq.nextval, #{userid}, '1', #{comment}, 1 ,#{type}, sysdate)
        ]]>
    </insert>
</mapper>